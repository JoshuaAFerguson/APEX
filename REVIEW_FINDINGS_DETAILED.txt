================================================================================
CODE REVIEW FINDINGS - v0.6.0 Feature Audit Implementation
================================================================================

STAGE: Review
DATE: 2026-03-11
STATUS: BLOCKED - Test Failures Prevent Completion

================================================================================
CRITICAL FINDINGS (BLOCKING)
================================================================================

1. VARIABLE SCOPE BINDING FAILURE - HIGH SEVERITY
   File: tests/v060-stack-documentation-verification.test.ts
   Lines: 28, 1090-1140+
   Error Type: "testProjectDir is not defined"
   Affected Tests: 25+ tests

   Issue: Helper functions reference outer-scope variable but fail at runtime

   Root Cause:
   - testProjectDir declared at describe block level (line 28)
   - Helper functions createNodeTypeScriptStack(), createReactStack(), etc.
     reference this variable in their function bodies
   - Variable binding fails when tests execute asynchronously

   Impact: Entire test file disabled - all tests fail with same error

   Fix: Pass testProjectDir as function parameter instead of using closure

   Example Fix:
   ============
   // BEFORE
   const testProjectDir = '/tmp/apex-stack-docs-test';
   async function createNodeTypeScriptStack() {
     await fs.writeFile(path.join(testProjectDir, ...));
   }

   // AFTER
   async function createNodeTypeScriptStack(testProjectDir: string) {
     await fs.writeFile(path.join(testProjectDir, ...));
   }

   Then call: await createNodeTypeScriptStack(testProjectDir);

================================================================================
HIGH SEVERITY FINDINGS
================================================================================

2. TEST ASSERTION MISMATCH - MULTIPLE FILES

   a) packages/cli/src/ui/components/__tests__/ToolCall.test.tsx
      Lines: 240, 248, 278, 294, 297, 425, 463, 509, 535, 540, 547, 555, 562, 677, 691, 703

      Issue: Comments added explaining expected behavior but don't verify
             assertions actually work with implementation

      Examples:
      - Line 240: formatDuration(1500) should return '1.5s' not '1500ms'
        Comment added but test needs verification

      - Line 509: formatInput() shows first string value, not '7 params'
        Comment added: "First param string has string value"
        But test still checks for 'string' when it might show param name

      - Line 703: Test allows up to 15 seconds for large output rendering
        Was 200ms threshold - change seems too permissive

      Action: Verify each assertion actually passes with current implementation

3. MOCK CONFIGURATION INCOMPLETENESS
   File: packages/orchestrator/src/codebase-intelligence/__tests__/indexer.test.ts
   Lines: 15-21, 35-39, 162-170, 620

   Issue: Import path changed from 'fs' to 'fs/promises' but implementation
          not fully updated across all test cases

   Problems:
   - Line 35: Mock changed to 'fs/promises' with direct exports
   - Line 162: Setup still assumes old pattern
   - Line 620: Mock resolver incomplete - missing return object wrapping

   Changes Made:
   OLD:
   vi.mock('fs', () => ({
     promises: {
       stat: vi.fn(),
       readFile: vi.fn(),
     },
   }));

   NEW:
   vi.mock('fs/promises', () => ({
     stat: vi.fn(),
     readFile: vi.fn(),
   }));

   Action: Verify all test cases correctly import and use mocked module

================================================================================
MEDIUM SEVERITY FINDINGS
================================================================================

4. TYPE CHANGE PROPAGATION
   File: packages/orchestrator/src/codebase-intelligence/__tests__/indexer.test.ts
   Lines: 503, 563

   Issue: Property renamed hasParseErrors → hasErrors

   Changes:
   - Line 503: ✓ FIXED: expect(result.files[0].hasErrors).toBe(true);
   - Line 563: ✓ FIXED: expect(result.files[0].hasErrors).toBe(true);

   But line 620 mock still incomplete:
   ```
   });  // Missing closing brace for mock extractor
   }    // Extra brace
   ```

   Action: Fix mock object syntax at line 620

5. MCP COMMAND TEST FAILURES
   File: packages/cli/src/commands/mcp.test.ts
   Failing Tests: 4 tests (show server configuration details, display management
                 commands, display marketplace commands in list, etc.)

   Issues:
   - Expected substring not in output
   - Coloring function error: __vite_ssr_import_0__.default.magenta
   - Mock setup for chalk/coloring library incorrect for ESM

   Examples:
   × expected 'CYAN:\n📦 Installed MCP Servers:\n\n …' to contain 'RED:disabled'
   × expected 'CYAN:\n📦 Installed MCP Servers:\n\n …' to contain 'GRAY:Management commands:'
   × expected 'CYAN:\n📦 MCP Marketplace - Available…' to contain 'GRAY:\n🔍 Marketplace commands:'

   Action: Update MCP command output format or fix test expectations

6. PERFORMANCE BENCHMARK FAILURE
   File: packages/orchestrator/src/codebase-intelligence/indexer.performance.test.ts
   Line: ~169
   Test: should handle large numbers of small files efficiently

   Issue: expected 400 to be 300

   This is backwards - seems like test environment is faster than expected
   OR the benchmark threshold should be adjusted

   Action: Review performance requirements or adjust CI timeout settings

================================================================================
LOW SEVERITY FINDINGS
================================================================================

7. DESIGN PATTERN - PROCESS PRIORITY MANAGEMENT
   File: packages/orchestrator/src/runner.ts
   Lines: 2236-2300

   Review: Implementation uses acceptable patterns for optional feature

   Strengths:
   ✓ Platform-aware (returns early on Windows)
   ✓ Error handling with try-catch (line 2243)
   ✓ Silent failure acceptable for background task
   ✓ Safe use of execFileSync (static parameters)
   ✓ Efficient process tree traversal

   Patterns to Note:
   - execFileSync('ps', ['-eo', 'pid,ppid,nice']): Safe but Unix-only
   - os.setPriority(pid, niceLevel): Graceful failure on permission error
   - Renice every 30s: May impact high-load systems

   Recommendation: Document as optional feature, not guaranteed to work

8. TEST FILE ORGANIZATION

   Untracked Files Created:
   - packages/cli/src/services/__tests__/SessionStore.comprehensive-audit.test.ts (39 KB)
   - packages/orchestrator/src/__tests__/autonomy-implementation-verification.test.ts
   - Multiple ADR and audit documentation files

   Action: Decide whether to commit or clean up these files

================================================================================
BUILD & COMPILATION STATUS
================================================================================

✅ TypeScript Build: PASS
   All packages compiled successfully
   Warnings: 2 non-critical Web UI linting (aria-invalid on button)

✅ Project Build: PASS
   Time: 31.5 seconds
   Output: 7 tasks successful, 2 cached

❌ Test Suite: FAIL
   Errors: 28+ test failures
   Blocking: Cannot verify implementation without test pass

================================================================================
ACCEPTANCE CRITERIA VERIFICATION
================================================================================

Required: Git status awareness
Status: ✓ PASS
Notes: Repository properly tracked, 49 commits ahead

Required: Project structure analysis
Status: ⚠ PARTIAL - Code present but tests blocked
Notes: Core types and schema additions verified in types.ts

Required: Apex map-codebase
Status: ⚠ PARTIAL - Implementation exists but unverified
Notes: Codebase intelligence features present, tests cannot verify

Required: Stack documentation
Status: ❌ FAIL
Notes: 25+ tests failing due to variable binding issue

Required: Real implementation verification
Status: ❌ FAIL
Notes: Cannot verify without passing tests

================================================================================
RECOMMENDATIONS BY PRIORITY
================================================================================

IMMEDIATE (Blocking):
1. Fix testProjectDir variable binding in v060-stack-documentation-verification.test.ts
   - Pass as function parameter to helper functions
   - Estimated effort: 30 minutes
   - Blocker for 25+ tests

2. Verify all ToolCall.test.tsx assertions match actual implementation
   - Run tests with focused output for each assertion
   - Estimated effort: 1-2 hours
   - Blocker for 10+ tests

3. Fix MCP command test output expectations
   - Update expected strings or fix command output
   - Estimated effort: 1 hour
   - Blocker for 4 tests

FOLLOW-UP (Next iteration):
1. Document process priority feature limitations
2. Add integration tests for process management on Unix
3. Review performance thresholds for CI environment

================================================================================
REVIEW CONCLUSION
================================================================================

STATUS: BLOCKED - Cannot Complete Review Stage
REASON: 28+ test failures must be resolved
NEXT ACTION: Return to Implementation Stage

The code changes are well-designed and follow good patterns, but test
failures prevent acceptance criteria verification. After test fixes are
completed, expected outcome is successful review completion.

Key Issues to Address:
1. Variable scope binding (25 tests)
2. Assertion mismatches (10 tests)
3. Mock configuration (3 tests)
4. Performance thresholds (1 test)

Estimated time to fix: 2-3 hours
Risk level: LOW (issues are test-only, not production code)

================================================================================
