================================================================================
CODE REVIEW: v0.1.0 CLI Commands Audit - DETAILED FINDINGS
================================================================================

SEVERITY LEVELS:
- HIGH: Critical bugs that prevent execution
- MEDIUM: Issues that violate design patterns or cause test failures
- LOW: Code quality improvements

================================================================================
ISSUE #1: MISSING EXPORT
================================================================================
FILE: packages/cli/src/index.ts
LINE: 5563
SEVERITY: HIGH

PROBLEM:
The function `executeNonInteractiveCommand` is defined but NOT exported, yet tests
import it expecting it to be available.

CURRENT CODE (Line 5563):
  async function executeNonInteractiveCommand(cmdName: string, args: string[]): Promise<void> {

EXPECTED CODE:
  export async function executeNonInteractiveCommand(cmdName: string, args: string[]): Promise<void> {

TEST FAILURE:
  TypeError: __vi_import_2__.executeNonInteractiveCommand is not a function
  At: packages/cli/src/commands/__tests__/cli-commands.implementation.test.ts:491

IMPACT: 2 tests fail
- Non-Interactive Command Execution > should execute commands non-interactively
- Non-Interactive Command Execution > should handle unknown commands gracefully

================================================================================
ISSUE #2: NULL REFERENCE IN STATUS COMMAND
================================================================================
FILE: packages/cli/src/index.ts
LINES: 475-478
SEVERITY: HIGH

PROBLEM:
The code assumes all tasks have a `usage` property, but it can be undefined.
When calculating sessionUsage, accessing `.estimatedCost` on undefined causes crash.

CURRENT CODE:
  const sessionUsage = allTasks.reduce((total, task) => ({
    totalTokens: total.totalTokens + (task.usage?.totalTokens || 0),
    estimatedCost: total.estimatedCost + (task.usage?.estimatedCost || 0),
  }), { totalTokens: 0, estimatedCost: 0 });

The issue: Fallback to 0 works for totalTokens, but mock object has no `usage` at all,
causing the reduce to fail when trying to read `.estimatedCost` on undefined.

TEST FAILURE:
  TypeError: Cannot read properties of undefined (reading 'estimatedCost')
  At: packages/cli/src/index.ts:509
  Triggered by test: packages/cli/src/commands/__tests__/cli-commands.implementation.test.ts:280

IMPACT: 1 test fails
- apex status Command Implementation > should have functional implementation

================================================================================
ISSUE #3: MISSING PROPERTY IN MOCK OBJECT
================================================================================
FILE: packages/cli/src/commands/__tests__/cli-commands.implementation.test.ts
LINES: 289-297
SEVERITY: HIGH

PROBLEM:
Mock task object doesn't include `createdAt` property, but the code tries to access it.

CURRENT MOCK (Lines 289-297):
  const mockTask = {
    id: 'task-123',
    description: 'Specific task',
    status: 'running',
    logs: [ /* ... */ ],
  }; // Missing: createdAt, usage, workflow, branchName

CODE THAT FAILS (index.ts:453):
  console.log(`Created: ${task.createdAt.toLocaleString()}`);

TEST FAILURE:
  TypeError: Cannot read properties of undefined (reading 'toLocaleString')
  At: packages/cli/src/index.ts:453

IMPACT: 1 test fails
- apex status Command Implementation > should handle specific task status lookup

================================================================================
ISSUE #4: MISSING ORCHESTRATOR METHOD MOCK
================================================================================
FILE: packages/cli/src/index.ts
LINES: 4546-4547
SEVERITY: HIGH

PROBLEM:
The `executeTask` function calls `ctx.orchestrator.on()` but the mock orchestrator
doesn't have this method defined.

CODE THAT FAILS (Lines 4546-4547):
  ctx.orchestrator.on('task:stage-changed', stageHandler);
  ctx.orchestrator.on('agent:tool-use', toolHandler);

MOCK DEFINITION (test file, line 70):
  orchestrator: {
    listTasks: vi.fn(),
    getTask: vi.fn(),
    createTask: vi.fn(),
  } as unknown as ApexOrchestrator, // Missing: on, off, respondToApproval

TEST FAILURE:
  TypeError: ctx2.orchestrator.on is not a function
  At: packages/cli/src/index.ts:4546

IMPACT: 2 tests fail
- apex run Command Implementation > should have functional implementation
- apex run Command Implementation > should handle run options properly

================================================================================
ISSUE #5: INCONSISTENT ERROR HANDLING - LOGS COMMAND
================================================================================
FILE: packages/cli/src/index.ts
LINES: 735-745
SEVERITY: MEDIUM

PROBLEM:
Handler silently returns on error instead of throwing. Tests expect errors to be
thrown for proper async error handling.

CURRENT CODE:
  const taskId = args[0];
  if (!taskId) {
    console.log(chalk.red('Usage: /logs <task_id>'));
    return; // ❌ Should throw
  }

  const task = await ctx.orchestrator.getTask(taskId);
  if (!task) {
    console.log(chalk.red(`Task not found: ${taskId}`));
    return; // ❌ Should throw
  }

EXPECTED CODE:
  const taskId = args[0];
  if (!taskId) {
    throw new Error('Task ID required. Usage: /logs <task_id>');
  }

  const task = await ctx.orchestrator.getTask(taskId);
  if (!task) {
    throw new Error(`Task not found: ${taskId}`);
  }

TEST FAILURE:
  AssertionError: promise resolved "undefined" instead of rejecting
  At: packages/cli/src/commands/__tests__/cli-commands.implementation.test.ts:466

IMPACT: 2 tests fail
- apex logs Command Implementation > should require task ID parameter
- apex logs Command Implementation > should handle non-existent task gracefully

================================================================================
ISSUE #6: INCONSISTENT ERROR HANDLING - RUN COMMAND
================================================================================
FILE: packages/cli/src/index.ts
LINES: 1124-1127
SEVERITY: MEDIUM

PROBLEM:
Handler silently returns when description is empty instead of throwing error.

CURRENT CODE:
  if (!description) {
    console.log(chalk.red('Usage: /run "<description>" [--workflow <name>] [--dry-run]'));
    return; // ❌ Should throw
  }

EXPECTED CODE:
  if (!description) {
    throw new Error('Task description required. Usage: /run "<description>" [--workflow <name>] [--dry-run]');
  }

TEST FAILURE:
  AssertionError: promise resolved "undefined" instead of rejecting
  At: packages/cli/src/commands/__tests__/cli-commands.implementation.test.ts:243

IMPACT: 1 test fails
- apex run Command Implementation > should validate task description is provided

================================================================================
ISSUE #7: INCONSISTENT ERROR HANDLING - UNINITIALIZED CONTEXT
================================================================================
FILE: packages/cli/src/index.ts
LINES: 1074-1076
SEVERITY: MEDIUM

PROBLEM:
Handler silently returns when not initialized instead of throwing error.

CURRENT CODE (run command):
  if (!ctx.initialized || !ctx.orchestrator) {
    console.log(chalk.red('APEX not initialized. Run /init first.'));
    return; // ❌ Should throw
  }

EXPECTED CODE:
  if (!ctx.initialized || !ctx.orchestrator) {
    throw new Error('APEX not initialized. Run /init first.');
  }

TEST FAILURE:
  AssertionError: promise resolved "undefined" instead of rejecting
  At: packages/cli/src/commands/__tests__/cli-commands.implementation.test.ts:565

IMPACT: 1 test fails
- Error Handling and Edge Cases > should handle uninitialized context

================================================================================
ISSUE #8: NULL CONFIG REFERENCE - AGENTS COMMAND
================================================================================
FILE: packages/cli/src/index.ts
LINE: 568
SEVERITY: MEDIUM

PROBLEM:
Handler accesses `config.agents?.disabled` without null-checking ctx.config first.

CURRENT CODE (Lines 562-576):
  const agents = await loadAgents(ctx.cwd);
  const config = ctx.config!; // Line 563 - assumes config is not null
  
  for (const [name, agent] of Object.entries(agents)) {
    const enabled = !config.agents?.disabled?.includes(name); // Line 568

WHEN config IS null:
  TypeError: Cannot read properties of null (reading 'agents')
  At: packages/cli/src/index.ts:568

EXPECTED CODE:
  const agents = await loadAgents(ctx.cwd);
  
  if (!ctx.config) {
    // Handle missing config gracefully
    console.log(chalk.red('Configuration not available'));
    return;
  }

  const config = ctx.config;
  for (const [name, agent] of Object.entries(agents)) {
    const enabled = !config.agents?.disabled?.includes(name);

TEST FAILURE:
  TypeError: Cannot read properties of null (reading 'agents')
  At: packages/cli/src/index.ts:568

IMPACT: 1 test fails
- Error Handling and Edge Cases > should handle missing configuration gracefully

================================================================================
ISSUE #9: TEST MOCK SETUP - FS FUNCTIONS
================================================================================
FILE: packages/cli/src/commands/__tests__/cli-commands.implementation.test.ts
LINES: 155, 156, 172, 173
SEVERITY: HIGH

PROBLEM:
Test uses vi.mock('fs/promises') but then tries to mock individual functions
that don't exist on the mock object.

CURRENT TEST CODE:
  vi.mock('fs/promises');
  // ...
  (fs.mkdir as Mock).mockResolvedValue(undefined);      // Line 155 ❌
  (fs.writeFile as Mock).mockResolvedValue(undefined);  // Line 156 ❌

PROBLEM:
After vi.mock('fs/promises'), fs.mkdir is just vi.fn(), not a function with
.mockResolvedValue method. Need to set up the mock differently.

EXPECTED APPROACH:
  vi.mock('fs/promises', () => ({
    mkdir: vi.fn().mockResolvedValue(undefined),
    writeFile: vi.fn().mockResolvedValue(undefined),
    // ... other functions
  }));

TEST FAILURE:
  TypeError: __vi_import_0__.mkdir.mockResolvedValue is not a function
  At: packages/cli/src/commands/__tests__/cli-commands.implementation.test.ts:155

IMPACT: 2 tests fail
- apex init Command Implementation > should have functional implementation
- apex init Command Implementation > should handle initialization arguments properly

================================================================================
SUMMARY OF FINDINGS
================================================================================

TOTAL ISSUES: 9
- HIGH SEVERITY: 6
- MEDIUM SEVERITY: 7 (some issues affect multiple categories)

TEST RESULTS:
- Total Tests: 30
- Passed: 17 (56%)
- Failed: 13 (44%)

BUILD STATUS: ✅ PASSED (TypeScript compilation successful)
TEST STATUS: ❌ FAILED (Critical tests failing)

================================================================================
COMMANDS STATUS MATRIX
================================================================================

apex init     | ✅ Implemented | ❌ 2 test failures  | Mock setup issues
apex run      | ✅ Implemented | ❌ 3 test failures  | Mock methods + error handling
apex status   | ✅ Implemented | ❌ 2 test failures  | Null references + mock properties
apex agents   | ✅ Implemented | ⚠️  Partial pass   | Null config reference
apex workflows| ✅ Implemented | ✅ All passing      | No issues
apex logs     | ✅ Implemented | ❌ 2 test failures  | Error handling inconsistency

ACCEPTANCE CRITERIA:
- ✅ All 6 commands exist
- ✅ All 6 have implementations (not stubs)
- ❌ Cannot verify functionality due to test failures
- ❌ Multiple runtime bugs identified

STATUS: ❌ NOT MET - Critical issues must be fixed

================================================================================
END OF DETAILED REVIEW
================================================================================
